Changes in version 2.2.4:
  o Suppresion importFrom(e1071,element) in NAMESPACE file 
  for ggplot2 dependency
  
Changes in version 2.2.3:
  o Corrections of the Authors@R field
  o Modif importFrom(e1071) for ggplot2 dependency

Changes in version 2.2.2:
  o Removing dependence to the emoa package (deprecated)
  o Supress the MRM fct as it requires the fct dominated_hypervolume()
  of the emoa package
  o Typo corrections in .Rd files
  o Changes from 'class()' to 'inherits()' in estimateSUR() fct
  
Changes in version 2.2.1:
  o Add doi number in the Description field
